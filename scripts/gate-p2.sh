#!/usr/bin/env bash
# Gate P2 — see DESIGN.md §4. Written at the START of phase P2, then made
# green. Exits 0 only when every criterion for the phase holds. A red gate
# blocks the next phase; there is no override flag on purpose.
set -euo pipefail
echo "gate-p2: not yet implemented — phase P2 has not started" >&2
exit 1
