#!/usr/bin/env bash
# Gate P3 — see DESIGN.md §4. Written at the START of phase P3, then made
# green. Exits 0 only when every criterion for the phase holds. A red gate
# blocks the next phase; there is no override flag on purpose.
set -euo pipefail
echo "gate-p3: not yet implemented — phase P3 has not started" >&2
exit 1
