# binaries & profiles
*.test
*.out
*.prof
/bin/
# spill-audit artifacts (archived per-commit under docs/ssa when wanted)
ssa.html
# bench scratch
bench/results/*.local.*
# machine-local remote execution targets (see .keel-hosts.example, docs/hosts.md)
/.keel-hosts
