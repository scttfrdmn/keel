- Phase / umbrella issue: #
- [ ] Gate script status pasted below (or "no gate change")
- [ ] Any new vector op has a scalar twin + differential test
- [ ] CHANGELOG.md `[Unreleased]` updated if user-visible
- [ ] Benchmarks (if claimed) include CPU + theoretical peak denominator

```
gate output here
```
