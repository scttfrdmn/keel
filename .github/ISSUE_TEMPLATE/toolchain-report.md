---
name: Toolchain field note
about: A GOEXPERIMENT=simd surprise (missing intrinsic, odd lowering, allocator behavior, API break)
labels: toolchain-report
---

**Toolchain** (go version + GOEXPERIMENT):

**Observation** (one paragraph):

**Minimal repro** (code + command + output):

**Impact on keel** (which phase/kernel, worked around or blocked):

**Upstream candidate?** If yes, sketch the golang/go issue title.

_Also add a row to `docs/toolchain-notes.md` in the same PR/commit._
