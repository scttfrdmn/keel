# Copyright 2026 Scott Friedman
# SPDX-License-Identifier: Apache-2.0
#
# Citations written in DESIGN.md's own notation that name a DIFFERENT document, declared
# per file and form, each carrying the number of sites it covers so an exemption cannot
# silently widen. An undeclared unresolvable citation is an error rather than a guess.
# Read by scripts/citation-lint.sh. Hand-maintained: there is no --write mode any more.
#
# Was docs/citation-targets.txt, whose PINS block was retired 2026-08-16 along with the
# pinning instrument (see that script's header, and DESIGN.md §5 rule 9 as amended). The
# name went with it: this file no longer records what a citation lands on, only which
# citations are not about DESIGN.md at all.
#
# FIVE OF THE NINE ARE LOAD-BEARING for resolution -- §3.3 and §4.3 name no item that
# exists in DESIGN.md, so without the declaration they are a false red. The other four
# (KERNEL.md's §5.2, spill-report's own §5.1-5.3) name forms that WOULD resolve against
# DESIGN.md, so they are declarations of MEANING that the lint cannot enforce: it would
# pass either way. #91 is that gap, open.
#
# AUTHORITY, kept because it is a dated fact and not a cache. On 2026-08-16 every
# DESIGN-bound citation site was read at the meaning level -- cited ordinal against the
# content the citing site actually invokes -- since no mechanical check establishes that.
# Census BEFORE the sweep: 120 sites; 8 naming another document; 112 DESIGN-bound, of
# which 92 land on the content the citing site invokes and 20 were mis-minted (18 x
# '§5.4' + DESIGN.md:129 '§7 rule 6' + l1_test.go:26 '§5.2'). The sweep rewrote 16 of the
# 20 to the explicit form and marked the other 4 as quotations, which document the defect
# and must not be normalised. Of the 92 already right, the 25 using the §X.Y shorthand
# were left byte-for-byte. That reading is what the current check rests on and does not
# re-establish; a later sweep of this size needs reading again.
#
# FILE<TAB>FORM<TAB>SITES<TAB>the document it actually cites
CHANGELOG.md	§3.3	1	docs/spill-report.md §3.3 — the spill accounting, not DESIGN.md
KERNEL.md	§3.3	1	docs/spill-report.md §3.3 — the spill accounting, not DESIGN.md
DESIGN.md	§4.3	1	Van Zee & van de Geijn, TOMS 2015 (BLIS) §4.3 — the Strsm recipe
CHANGELOG.md	§4.3	2	Van Zee & van de Geijn, TOMS 2015 (BLIS) §4.3 — the Strsm recipe
internal/block/tri.go	§4.3	1	Van Zee & van de Geijn, TOMS 2015 (BLIS) §4.3 — the Strsm recipe
KERNEL.md	§5.2	1	docs/spill-report.md §5.2-5.3 — the en dash hides the §5.3 half from the scanner
docs/spill-report.md	§5.1	1	docs/spill-report.md's own §5.1 — self-reference
docs/spill-report.md	§5.2	1	docs/spill-report.md's own §5.2 — self-reference
docs/spill-report.md	§5.3	1	docs/spill-report.md's own §5.3 — self-reference
